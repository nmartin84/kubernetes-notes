---
tags:
  - k8s/nodes
  - k8s/clusters
---
This node manages the cluster by exposing the Kubernetes API and handling cluster events, such as scaling the number of Pods. In production environments, multiple control plane nodes (typically three or more) are used for high availability. 